version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./runs:/app/runs
      - ./logs:/app/logs
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    gpus: all
    restart: unless-stopped
